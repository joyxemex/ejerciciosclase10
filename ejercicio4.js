//● Ejercicio 4 – Tipo de Datos: Declarar 4 variables utilizando la palabra 
// reservada let y asignarles valores según el tipo de dato que sugiera su nombre. 
// Además, realizar operaciones y validaciones adicionales sobre estos valores.
//Pasos a seguir:
//Declaración de Variables:
//1. Declara una variable llamada verdadero y asígnale un dato de tipo booleano (true o false).
//2. Declara una variable llamada texto y asígnale un string de tu elección (puedes usar cualquier mensaje que prefi eras).
//3. Declara una variable llamada numero y asígnale un dato numérico (puedes usar cualquier número entero o decimal).
//4. Declara una variable llamada nada y asígnale un valor que represente la ausencia de valor en JavaScript.
//Operaciones y Validaciones:
//1. Realiza una operación matemática usando la variable número, por ejemplo, suma o resta con otro número de tu elección.
//2. Concatena el “string” de la variable texto con otro mensaje adicional.
//3. Verifica si la variable “verdadero” es true o false y muestra un mensaje diferente según el resultado.
//4. Implementa una condición para verifi car si la variable nada es null y muestra un mensaje si es así.

//llamamos a la libreria
const prompt = require('prompt-sync')();

let verdadero = verdadero;
let texto = ("esto es un texto");
let numero = 8;
let nada = null; 
//1. Realiza una operación matemática usando la variable número, por ejemplo, suma o resta con otro número de tu elección.
let suma = numero + 8;
console.log("Resultado de la suma: " + suma);

//2. Concatena el “string” de la variable texto con otro mensaje adicional.
let textoCompleto = texto + " esto es otro texto";
console.log(mensajeCompleto);

//3. Verifica si la variable “verdadero” es true o false y muestra un mensaje diferente según el resultado.

if (verdadero === verdadero) {
    console.log("La variable es verdadero (true).");
} else {
    console.log("La variable es falsa (false).");
}

